chore: clean build cache and reinstall dependencies

- Remove .next build folder
- Remove node_modules and reinstall dependencies
- Clean pnpm cache for fresh build

This ensures a clean build environment for Vercel deployment.
